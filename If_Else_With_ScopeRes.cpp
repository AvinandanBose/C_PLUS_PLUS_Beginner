#include<iostream>
using namespace std;
int main(){
    int n;
    cout<<"Enter a number:" <<"\n";
    cin>>n;
    if(n>1)::cout<<"The number is greater than 1"<<"\n"; // indentation 
    else::cout<<"The number is less than 1"<<"\n"; // indentation
    return 0;
}